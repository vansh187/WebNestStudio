// Spring Boot module — hand-written lesson content.
// Keys are slugs matching the Spring Boot topics list.
export const springBootContent = {
  'project-setup': {
    title: 'Spring Boot Project Setup',
    intro: `Spring Boot is an opinionated framework built on top of the Spring Framework that removes most of the manual configuration Spring traditionally required. Instead of wiring beans by hand and configuring an application server, you get a production-ready application by adding a handful of dependencies and letting Spring Boot's auto-configuration do the rest.

The fastest way to start a new project is Spring Initializr (start.spring.io), a web tool (also built into most IDEs) that generates a ready-to-run project skeleton. You choose the build tool (Maven or Gradle), the language (Java, Kotlin, or Groovy), the Spring Boot version, and the dependencies (starters) you need, and it produces a downloadable zip with the correct folder structure and build file already configured.`,
    sections: [
      {
        heading: 'Standard Project Structure',
        body: `A generated Spring Boot project follows a conventional Maven/Gradle layout that the framework expects by default.`,
        list: [
          '<code>src/main/java</code> — your application source code, rooted in a base package (e.g. com.webnest.app).',
          '<code>src/main/resources</code> — application.properties or application.yml, static assets, and templates.',
          '<code>src/test/java</code> — test classes, mirroring the main package structure.',
          '<code>pom.xml</code> (Maven) or <code>build.gradle</code> (Gradle) — declares the Spring Boot parent/plugin and dependencies.',
        ],
      },
      {
        heading: 'The Main Application Class and @SpringBootApplication',
        body: `Every Spring Boot app has exactly one entry-point class annotated with <code>@SpringBootApplication</code>, which is itself a convenience annotation combining three separate annotations: <code>@Configuration</code> (marks the class as a source of bean definitions), <code>@EnableAutoConfiguration</code> (triggers Spring Boot's auto-configuration mechanism), and <code>@ComponentScan</code> (scans the current package and sub-packages for components, controllers, services, and repositories). This is why placing your entry class at the root package matters — anything outside that package tree won't be scanned by default.`,
      },
      {
        heading: 'Running the Application',
        body: `The main method calls <code>SpringApplication.run(YourAppClass.class, args)</code>, which bootstraps the Spring ApplicationContext, starts the embedded server (Tomcat by default for web apps), and keeps the JVM alive to serve requests. You can run the app from your IDE, with <code>mvn spring-boot:run</code>, or as a standalone executable jar produced by <code>mvn package</code>.`,
      },
    ],
    examples: [
      {
        caption: 'Minimal Spring Boot entry point generated by Initializr',
        code: `package com.webnest.app;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class WebnestAppApplication {

    public static void main(String[] args) {
        SpringApplication.run(WebnestAppApplication.class, args);
    }
}`,
        output: `  .   ____          _            __ _ _
 /\\\\ / ___'_ __ _ _(_)_ __  __ _
( ( )\\___ | '_ | '_| | '_ \\/ _\` |
 \\\\/  ___)| |_)| | | | | || (_| |
  '  |____| .__|_| |_|_| |_\\__, |
 =========|_|==============|___/=

 :: Spring Boot ::               (v3.2.4)

... Tomcat started on port(s): 8080 (http)
... Started WebnestAppApplication in 1.842 seconds`,
      },
    ],
    commonMistakes: [
      'Placing controllers or services in a package outside (or a sibling of) the package containing the @SpringBootApplication class — component scanning will silently skip them.',
      'Deleting or renaming the auto-generated main application class without understanding that it is the single source of auto-configuration and component scanning.',
      'Manually configuring beans that Spring Boot already auto-configures, leading to duplicate bean definition conflicts.',
    ],
    keyPoints: [
      'Spring Initializr generates a ready-to-run project with the correct structure and build file for your chosen dependencies.',
      '@SpringBootApplication = @Configuration + @EnableAutoConfiguration + @ComponentScan.',
      'The entry class should sit at the root of your package tree so component scanning covers everything beneath it.',
      'SpringApplication.run() bootstraps the ApplicationContext and starts the embedded server in one call.',
    ],
  },

  starters: {
    title: 'Spring Boot Starters',
    intro: `Starters are curated dependency descriptors that pull in everything typically needed for a given kind of functionality, with versions that are already tested to work together. Instead of manually choosing compatible versions of Spring MVC, Jackson, an embedded servlet container, and validation libraries, you add one starter and Spring Boot's dependency management resolves a known-good set for you.

This matters because version mismatches between related libraries are one of the most common sources of hard-to-diagnose runtime errors in Java projects. Starters exist specifically to eliminate that class of problem.`,
    sections: [
      {
        heading: 'Commonly Used Starters',
        body: `Each starter is named after the capability it adds, following the pattern spring-boot-starter-*.`,
        list: [
          '<code>spring-boot-starter-web</code> — builds RESTful/MVC web applications using Spring MVC, with an embedded Tomcat server and Jackson for JSON.',
          '<code>spring-boot-starter-data-jpa</code> — Spring Data JPA on top of Hibernate, for working with relational databases through repository interfaces.',
          '<code>spring-boot-starter-security</code> — Spring Security, adding authentication and authorization with sensible defaults (e.g. every endpoint requires login unless configured otherwise).',
          '<code>spring-boot-starter-validation</code> — Bean Validation (Jakarta Validation) support for @Valid-based request validation.',
          '<code>spring-boot-starter-test</code> — JUnit 5, Mockito, AssertJ, and Spring Test, all pre-wired for testing (included by default in generated projects).',
        ],
      },
      {
        heading: 'How Starters Affect the Classpath',
        body: `Adding a starter to your pom.xml or build.gradle changes what is on the classpath, and that in turn changes what auto-configuration activates. For example, spring-boot-starter-data-jpa brings in Hibernate and the JPA API, which makes classes like <code>EntityManager</code> and <code>DataSource</code> available — and that presence is exactly what Spring Boot's auto-configuration checks for before wiring up a JPA-based persistence layer automatically.`,
      },
    ],
    examples: [
      {
        caption: 'Declaring starters in a Maven pom.xml',
        code: `<dependencies>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
        <groupId>com.h2database</groupId>
        <artifactId>h2</artifactId>
        <scope>runtime</scope>
    </dependency>
</dependencies>`,
        output: '(No console output — this changes the resolved dependency tree; run "mvn dependency:tree" to confirm Tomcat, Jackson, Hibernate, and HikariCP are now present.)',
      },
    ],
    commonMistakes: [
      'Manually specifying versions for individual libraries already managed by a starter, which can reintroduce the version-conflict problems starters are designed to avoid.',
      'Adding spring-boot-starter-security "just in case" and being surprised every endpoint suddenly returns 401 Unauthorized — the starter enables security by default the moment it is on the classpath.',
      'Forgetting that starters are transitive — removing a starter can silently remove auto-configuration you were relying on elsewhere.',
    ],
    keyPoints: [
      'Starters bundle compatible sets of dependencies for a specific concern (web, data, security, testing).',
      'spring-boot-starter-web, -data-jpa, and -security are the most commonly used starters in real applications.',
      'What is on the classpath (via starters) directly determines which auto-configuration classes activate.',
      'Prefer starters over hand-picking individual library versions to avoid compatibility issues.',
    ],
  },

  'auto-configuration': {
    title: 'Spring Boot Auto-Configuration',
    intro: `Auto-configuration is the mechanism that makes Spring Boot feel "magic" the first time you use it: add spring-boot-starter-data-jpa and a DataSource, and a fully working JPA setup — connection pool, EntityManagerFactory, transaction manager — appears without you writing a single @Bean method. Understanding how this actually works removes the magic and replaces it with a predictable rule set.`,
    sections: [
      {
        heading: '@EnableAutoConfiguration and Conditional Beans',
        body: `@EnableAutoConfiguration (bundled inside @SpringBootApplication) tells Spring Boot to scan a list of auto-configuration classes registered in the classpath (via META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports in modern versions) and conditionally apply each one. Each auto-configuration class is guarded by conditional annotations that decide, at startup, whether it should activate.`,
        list: [
          '<code>@ConditionalOnClass</code> — activates only if a specific class is present on the classpath (e.g. only configure JPA support if EntityManager.class exists).',
          '<code>@ConditionalOnMissingBean</code> — activates only if you have not already defined your own bean of that type, so your explicit configuration always wins over the default.',
          '<code>@ConditionalOnProperty</code> — activates only if a specific application property is set to a given value.',
          '<code>@ConditionalOnWebApplication</code> — activates only inside a web application context, not a plain CLI application.',
        ],
      },
      {
        heading: 'Overriding Auto-Configuration',
        body: `Because most auto-configuration is guarded by @ConditionalOnMissingBean, you can override any default simply by defining your own bean of the same type in a @Configuration class — Spring Boot's own bean backs off automatically. This "sensible defaults, easy to override" pattern is the core design philosophy behind the entire framework.`,
      },
      {
        heading: 'Inspecting What Auto-Configured',
        body: `When auto-configuration behaves unexpectedly, enabling debug logging (<code>--debug</code> flag or <code>debug=true</code> in application.properties) prints an auto-configuration report at startup listing every candidate class as "Positive match" or "Negative match," along with the exact condition that decided it — this is the single most useful diagnostic tool for auto-configuration issues.`,
      },
    ],
    examples: [
      {
        caption: 'Overriding an auto-configured bean with your own',
        code: `@Configuration
public class WebConfig {

    // Because ObjectMapper auto-configuration is @ConditionalOnMissingBean,
    // defining this bean replaces Spring Boot's default JSON mapper.
    @Bean
    public ObjectMapper objectMapper() {
        ObjectMapper mapper = new ObjectMapper();
        mapper.registerModule(new JavaTimeModule());
        mapper.disable(SerializationFeature.WRITE_DATES_AS_TIMESTAMPS);
        return mapper;
    }
}`,
        output: '(No console output at request time — verify with --debug at startup: "ObjectMapper defined user-supplied bean" appears in the auto-configuration report.)',
      },
    ],
    commonMistakes: [
      'Assuming auto-configuration always "just works" and skipping the --debug report when something behaves unexpectedly, instead of guessing.',
      'Defining a bean of a slightly different type than Spring Boot expects, so @ConditionalOnMissingBean does not detect it and you end up with two competing beans.',
      'Believing @EnableAutoConfiguration scans your own code for components — it only evaluates registered auto-configuration classes, not your @Component classes (that is @ComponentScan\'s job).',
    ],
    keyPoints: [
      '@EnableAutoConfiguration conditionally applies pre-built configuration classes based on what is on the classpath and what beans already exist.',
      '@ConditionalOnClass, @ConditionalOnMissingBean, and @ConditionalOnProperty are the most common conditions guarding auto-configuration.',
      'Defining your own @Bean of the same type is the standard way to override an auto-configured default.',
      'Run with --debug to print the auto-configuration match/no-match report when behavior is unclear.',
    ],
  },

  'configuration-properties': {
    title: 'Spring Boot Configuration Properties',
    intro: `Spring Boot applications are configured externally rather than by editing code — you set values in application.properties or application.yml (or environment variables, command-line arguments, and more), and Spring Boot binds them into your running application. This lets the exact same jar behave differently in development, staging, and production without a rebuild.`,
    sections: [
      {
        heading: 'application.properties vs application.yml',
        body: `Both files live in src/main/resources and serve the same purpose — key-value configuration — but with different syntax. Properties files use dot-separated flat keys (server.port=8081); YAML uses nested indentation, which reads better for deeply structured configuration. You can use either, but not both for the same key in the same profile (if both exist, .properties takes precedence over .yml in default Spring Boot resolution).`,
      },
      {
        heading: 'Injecting Individual Values with @Value',
        body: `For a single simple value, <code>@Value("\${property.name}")</code> injects it directly into a field. This works well for one-off values but becomes unwieldy once you have more than a few related settings, since each one needs its own annotation and there's no compile-time grouping or validation.`,
      },
      {
        heading: 'Type-Safe Binding with @ConfigurationProperties',
        body: `For a related group of settings, @ConfigurationProperties binds an entire prefix of properties onto a strongly typed Java class in one step, with support for nested objects, lists, and maps. The class must be registered as a bean, typically via @ConfigurationProperties combined with @Component, or explicitly with @EnableConfigurationProperties on a configuration class.`,
      },
    ],
    examples: [
      {
        caption: 'Binding a group of related properties with @ConfigurationProperties',
        code: `# application.yml
app:
  mail:
    host: smtp.webnest.com
    port: 587
    retry-attempts: 3

---

@ConfigurationProperties(prefix = "app.mail")
@Component
public class MailProperties {
    private String host;
    private int port;
    private int retryAttempts;

    // getters and setters required for binding
    public String getHost() { return host; }
    public void setHost(String host) { this.host = host; }
    public int getPort() { return port; }
    public void setPort(int port) { this.port = port; }
    public int getRetryAttempts() { return retryAttempts; }
    public void setRetryAttempts(int retryAttempts) { this.retryAttempts = retryAttempts; }
}

@Service
public class MailService {
    private final MailProperties properties;

    public MailService(MailProperties properties) {
        this.properties = properties;
    }

    public void printConfig() {
        System.out.println(properties.getHost() + ":" + properties.getPort()
            + " retries=" + properties.getRetryAttempts());
    }
}`,
        output: 'smtp.webnest.com:587 retries=3',
      },
    ],
    commonMistakes: [
      'Forgetting getters/setters on a @ConfigurationProperties class, which silently breaks binding since Spring uses standard JavaBean conventions.',
      'Mixing kebab-case, camelCase, and snake_case inconsistently — Spring Boot\'s relaxed binding accepts several forms, but readability suffers without a consistent style (kebab-case is conventional in YAML).',
      'Using @Value scattered across many classes for what is really one configuration group, instead of consolidating into a single @ConfigurationProperties class.',
      'Not adding spring-boot-configuration-processor, which means the IDE has no autocomplete or metadata for custom properties.',
    ],
    keyPoints: [
      'application.properties and application.yml both configure the app externally; .properties wins over .yml if both define the same key.',
      '@Value injects single values; @ConfigurationProperties binds a whole related group into a typed class.',
      'A @ConfigurationProperties class needs standard getters/setters and must be registered as a Spring bean.',
      'Externalized configuration lets the same build run correctly in different environments.',
    ],
  },

  profiles: {
    title: 'Spring Boot Profiles',
    intro: `Real applications need different configuration for local development, automated testing, staging, and production — different database URLs, different logging levels, different feature flags. Spring Profiles let you define named sets of configuration and beans that only activate for a chosen environment, without maintaining separate codebases.`,
    sections: [
      {
        heading: 'Profile-Specific Configuration Files',
        body: `Alongside the default application.properties (or application.yml), you can create application-{profile}.properties files — for example application-dev.properties and application-prod.properties. Values in the profile-specific file override or add to the base file when that profile is active. In application.yml, the same effect is achieved with multi-document files separated by <code>---</code> and a <code>spring.config.activate.on-profile</code> key.`,
      },
      {
        heading: 'Activating a Profile',
        body: `The active profile is set with the property <code>spring.profiles.active=dev</code>, which can be provided in application.properties, as a JVM argument (<code>-Dspring.profiles.active=prod</code>), as an environment variable (<code>SPRING_PROFILES_ACTIVE=prod</code>), or as a command-line argument when launching the jar. Multiple profiles can be active simultaneously as a comma-separated list, and later ones override earlier ones on conflicting keys.`,
      },
      {
        heading: 'Profile-Specific Beans',
        body: `Beyond properties, the <code>@Profile("dev")</code> annotation on a @Bean method or @Component class restricts that bean to only be created when the named profile is active — useful for swapping a real payment gateway client for a mock one during local development and testing.`,
      },
    ],
    examples: [
      {
        caption: 'Profile-specific datasource configuration and a profile-restricted bean',
        code: `// application-dev.properties
spring.datasource.url=jdbc:h2:mem:devdb
logging.level.root=DEBUG

// application-prod.properties
spring.datasource.url=jdbc:postgresql://db.internal:5432/webnest
logging.level.root=WARN

// PaymentConfig.java
@Configuration
public class PaymentConfig {

    @Bean
    @Profile("dev")
    public PaymentGateway mockPaymentGateway() {
        return new MockPaymentGateway();
    }

    @Bean
    @Profile("prod")
    public PaymentGateway stripePaymentGateway() {
        return new StripePaymentGateway();
    }
}

// Run with: java -jar app.jar --spring.profiles.active=dev`,
        output: `... The following 1 profile is active: "dev"
... HikariPool-1 - Starting... url=jdbc:h2:mem:devdb
... Started WebnestAppApplication in 1.65 seconds`,
      },
    ],
    commonMistakes: [
      'Forgetting to set spring.profiles.active anywhere and being confused why "default" configuration always loads instead of the intended dev/prod file.',
      'Duplicating every property in each profile file instead of putting shared defaults in application.properties and only the differences in the profile-specific files.',
      'Hardcoding "prod" behavior directly in code with if-statements instead of using @Profile beans or profile-specific properties.',
      'Committing a profile file containing real production secrets (passwords, API keys) directly into version control.',
    ],
    keyPoints: [
      'application-{profile}.properties/yml files override the base configuration when that profile is active.',
      'spring.profiles.active selects the active profile(s), settable via property, environment variable, or command-line argument.',
      '@Profile restricts a bean definition to specific named profiles, useful for swapping implementations between environments.',
      'Keep shared configuration in the base file and only environment-specific differences in profile files.',
    ],
  },

  'rest-apis': {
    title: 'Building REST APIs with Spring Boot',
    intro: `Spring MVC, combined with Spring Boot's auto-configuration, is the standard way to expose RESTful HTTP endpoints in a Java backend. A REST controller maps incoming HTTP requests to Java methods, and Spring handles serialization, deserialization, routing, and status codes based on annotations.`,
    sections: [
      {
        heading: '@RestController and Mapping Annotations',
        body: `@RestController is a specialization of @Controller that adds @ResponseBody to every method, meaning return values are written directly to the HTTP response body (typically as JSON, via Jackson) instead of being resolved as a view name. Method-level mapping annotations tie a method to an HTTP verb and path.`,
        list: [
          '<code>@GetMapping("/api/products")</code> — handles HTTP GET, typically for retrieving data.',
          '<code>@PostMapping("/api/products")</code> — handles HTTP POST, typically for creating a resource.',
          '<code>@PutMapping("/api/products/{id}")</code> — handles HTTP PUT, typically for a full update.',
          '<code>@DeleteMapping("/api/products/{id}")</code> — handles HTTP DELETE, for removing a resource.',
        ],
      },
      {
        heading: 'Reading Request Data',
        body: `<code>@PathVariable</code> extracts a value from the URL path (e.g. the {id} in /products/{id}), <code>@RequestParam</code> reads a query-string parameter (e.g. ?page=2), and <code>@RequestBody</code> deserializes the incoming JSON request body directly into a Java object using Jackson.`,
      },
      {
        heading: 'Controlling the Response with ResponseEntity',
        body: `Returning a plain object always responds with HTTP 200 and that object serialized as JSON. When you need to control the exact status code or headers — 201 Created after a POST, 404 Not Found when a resource doesn't exist — wrap the response in <code>ResponseEntity&lt;T&gt;</code>, which gives full control over status, headers, and body together.`,
      },
    ],
    examples: [
      {
        caption: 'A REST controller with full CRUD-style mappings using ResponseEntity',
        code: `@RestController
@RequestMapping("/api/products")
public class ProductController {

    private final ProductService service;

    public ProductController(ProductService service) {
        this.service = service;
    }

    @GetMapping("/{id}")
    public ResponseEntity<Product> getById(@PathVariable Long id) {
        return service.findById(id)
                .map(ResponseEntity::ok)
                .orElse(ResponseEntity.notFound().build());
    }

    @PostMapping
    public ResponseEntity<Product> create(@RequestBody Product product) {
        Product saved = service.save(product);
        return ResponseEntity.status(HttpStatus.CREATED).body(saved);
    }

    @GetMapping
    public List<Product> list(@RequestParam(defaultValue = "0") int page) {
        return service.findPage(page);
    }
}`,
        output: `GET /api/products/5   -> 200 OK   {"id":5,"name":"Wireless Mouse","price":19.99}
POST /api/products    -> 201 Created {"id":6,"name":"USB Hub","price":24.5}
GET /api/products?page=0 -> 200 OK [{"id":1,...}, {"id":2,...}]`,
      },
    ],
    commonMistakes: [
      'Using @Controller instead of @RestController and forgetting @ResponseBody, causing Spring to try (and fail) to resolve the return value as a view template name.',
      'Returning the raw entity object for every case instead of ResponseEntity, which makes it impossible to return a proper 404 or 201 with the right semantics.',
      'Mismatching @PathVariable name and the {placeholder} in the mapping path when they are not identical identifiers, without using the explicit @PathVariable("id") form.',
      'Putting business logic directly inside the controller method instead of delegating to a service layer, which makes the class hard to test and reuse.',
    ],
    keyPoints: [
      '@RestController = @Controller + @ResponseBody, meaning return values are serialized straight to the response body.',
      '@GetMapping/@PostMapping/@PutMapping/@DeleteMapping map HTTP verbs to controller methods.',
      '@PathVariable reads URL segments, @RequestParam reads query parameters, @RequestBody deserializes the JSON body.',
      'ResponseEntity<T> gives full control over status code, headers, and body for the response.',
    ],
  },

  dtos: {
    title: 'DTOs in Spring Boot (Data Transfer Objects)',
    intro: `A DTO (Data Transfer Object) is a plain object used to carry data between layers of an application — most commonly between the HTTP layer and the outside world — without exposing your internal domain/entity model directly. In a Spring Boot REST API, this means your @RestController methods should typically accept and return DTOs, not JPA @Entity classes.`,
    sections: [
      {
        heading: 'Why Not Just Return the Entity?',
        body: `It's tempting to return a JPA entity directly from a controller since Jackson will happily serialize it, but this couples your public API contract to your database schema. Any column you add, rename, or remove on the entity immediately (and often unintentionally) changes your API response. Entities can also carry lazy-loaded associations that throw LazyInitializationException when serialized outside a transaction, and they may expose fields (password hashes, internal flags, audit columns) that should never leave the server.`,
        list: [
          'Entities model persistence (tables, relationships, JPA annotations); DTOs model the API contract (exactly what a client should see or send).',
          'DTOs let you shape a response differently than storage — combining fields from multiple entities, hiding sensitive data, or renaming fields for the client.',
          'Changing your database schema does not have to break your API if DTOs sit between them.',
        ],
      },
      {
        heading: 'Request DTOs vs Response DTOs',
        body: `It's common (and good practice) to use separate DTO classes for incoming requests and outgoing responses, since the fields a client is allowed to send (e.g. name, email) often differ from what a client should receive back (e.g. id, name, email, createdAt — but never a password). Mapping between entity and DTO is usually done manually in a service/mapper class, or with a library like MapStruct for larger projects.`,
      },
    ],
    examples: [
      {
        caption: 'Separate request/response DTOs mapped to and from a JPA entity',
        code: `// Entity - persistence model, never exposed directly
@Entity
public class User {
    @Id @GeneratedValue
    private Long id;
    private String name;
    private String email;
    private String passwordHash; // must never leave the server
    private LocalDateTime createdAt;
    // getters/setters omitted
}

// Request DTO - only what a client is allowed to submit
public record CreateUserRequest(String name, String email, String password) {}

// Response DTO - only what a client should see
public record UserResponse(Long id, String name, String email, LocalDateTime createdAt) {}

@RestController
@RequestMapping("/api/users")
public class UserController {

    private final UserService service;
    public UserController(UserService service) { this.service = service; }

    @PostMapping
    public ResponseEntity<UserResponse> create(@RequestBody CreateUserRequest request) {
        User saved = service.createUser(request);
        UserResponse body = new UserResponse(saved.getId(), saved.getName(),
                saved.getEmail(), saved.getCreatedAt());
        return ResponseEntity.status(HttpStatus.CREATED).body(body);
    }
}`,
        output: `POST /api/users {"name":"Asha","email":"asha@webnest.com","password":"secret123"}
-> 201 Created {"id":1,"name":"Asha","email":"asha@webnest.com","createdAt":"2026-09-21T10:15:30"}`,
      },
    ],
    commonMistakes: [
      'Returning JPA entities directly from controllers, leaking internal fields and risking LazyInitializationException on unfetched associations.',
      'Using a single DTO class for both requests and responses, which forces awkward compromises (e.g. an id field that must be ignored on create).',
      'Putting JPA annotations (@Entity, @Id) on what is meant to be a DTO, blurring the line between persistence and API contract.',
      'Manually copying every field with dozens of getter/setter calls in large projects instead of adopting a mapping library once the entity graph grows.',
    ],
    keyPoints: [
      'DTOs decouple the API contract from the database schema and internal domain model.',
      'Never return JPA entities directly from a @RestController — map them to response DTOs first.',
      'Separate request and response DTOs let each side expose exactly the fields it should.',
      'Java records are a concise, idiomatic way to write immutable DTOs.',
    ],
  },

  validation: {
    title: 'Request Validation in Spring Boot',
    intro: `Never trust input that arrives over HTTP. Spring Boot integrates with Bean Validation (the Jakarta Validation specification, implemented by Hibernate Validator) so you can declare validation rules directly on a DTO's fields with annotations, and have Spring enforce them automatically before your controller method body even runs.`,
    sections: [
      {
        heading: 'Enabling Validation and Common Annotations',
        body: `Add spring-boot-starter-validation to your dependencies, then annotate DTO fields with constraint annotations from jakarta.validation.constraints.`,
        list: [
          '<code>@NotNull</code> — the value must not be null.',
          '<code>@NotBlank</code> — a String must not be null and must contain at least one non-whitespace character.',
          '<code>@Size(min, max)</code> — bounds the length of a String or the size of a collection.',
          '<code>@Email</code> — the String must be a well-formed email address.',
          '<code>@Min</code> / <code>@Max</code> — numeric lower/upper bounds.',
          '<code>@Positive</code> / <code>@PositiveOrZero</code> — numeric sign constraints.',
        ],
      },
      {
        heading: 'Triggering Validation with @Valid',
        body: `Adding <code>@Valid</code> before a <code>@RequestBody</code> parameter in a controller method tells Spring to run Bean Validation on the deserialized object before the method executes. If any constraint fails, Spring throws a <code>MethodArgumentNotValidException</code> and, by default, responds with HTTP 400 Bad Request and a body describing each failed field — before your business logic ever sees the invalid data.`,
      },
      {
        heading: 'Customizing the Validation Error Response',
        body: `The default 400 response is functional but verbose and inconsistent across projects. Pairing @Valid with a @ControllerAdvice/@ExceptionHandler for MethodArgumentNotValidException lets you shape a clean, consistent error payload (field name, rejected value, message) instead of exposing Spring's raw exception structure.`,
      },
    ],
    examples: [
      {
        caption: 'Validating a request DTO with @Valid and Bean Validation annotations',
        code: `public record CreateUserRequest(
    @NotBlank(message = "Name is required") String name,
    @Email(message = "Email must be valid") @NotBlank String email,
    @Size(min = 8, message = "Password must be at least 8 characters") String password
) {}

@RestController
@RequestMapping("/api/users")
public class UserController {

    @PostMapping
    public ResponseEntity<String> create(@Valid @RequestBody CreateUserRequest request) {
        return ResponseEntity.status(HttpStatus.CREATED).body("User created: " + request.name());
    }
}

// POST /api/users {"name":"", "email":"not-an-email", "password":"123"}`,
        output: `-> 400 Bad Request
{
  "errors": [
    "Name is required",
    "Email must be valid",
    "Password must be at least 8 characters"
  ]
}`,
      },
    ],
    commonMistakes: [
      'Forgetting @Valid on the controller parameter — the constraint annotations exist on the DTO but are silently never checked.',
      'Forgetting to add spring-boot-starter-validation, so @NotNull/@NotBlank/etc. compile but have no runtime effect.',
      'Validating only at the controller layer and assuming that protects the database — internal service calls or batch jobs bypassing the controller still need their own checks.',
      'Not handling MethodArgumentNotValidException, leaving clients to parse Spring\'s default verbose error structure.',
    ],
    keyPoints: [
      'Bean Validation annotations (@NotNull, @NotBlank, @Size, @Email, @Min/@Max) declare rules directly on DTO fields.',
      '@Valid on a @RequestBody parameter triggers validation automatically before the controller method runs.',
      'Failed validation throws MethodArgumentNotValidException, which defaults to an HTTP 400 response.',
      'Pair @Valid with a global exception handler to produce a clean, consistent validation error format.',
    ],
  },

  'exception-handling': {
    title: 'Global Exception Handling in Spring Boot',
    intro: `Scattering try-catch blocks across every controller method produces inconsistent error responses and duplicated logic. Spring provides a centralized way to handle exceptions for an entire application: @ControllerAdvice combined with @ExceptionHandler methods, which intercept exceptions thrown anywhere in the request-handling flow and convert them into a single, consistent error response format.`,
    sections: [
      {
        heading: '@ControllerAdvice and @ExceptionHandler',
        body: `A class annotated with <code>@ControllerAdvice</code> (or <code>@RestControllerAdvice</code>, which adds @ResponseBody automatically) applies across all controllers in the application. Inside it, each method annotated with <code>@ExceptionHandler(SomeException.class)</code> handles that specific exception type whenever it propagates out of any controller, letting you build error handling once instead of per-endpoint.`,
      },
      {
        heading: 'Mapping Exceptions to HTTP Status Codes',
        body: `Different failure types should map to different HTTP status codes — a "resource not found" condition should return 404, a validation failure 400, an authorization failure 403, and an unexpected server error 500. Using <code>@ResponseStatus</code> on a custom exception class, or explicitly building a <code>ResponseEntity</code> inside the handler method, lets you control this precisely rather than letting every exception fall through to a generic 500.`,
      },
      {
        heading: 'Designing a Consistent Error Body',
        body: `Client applications benefit enormously from every error response sharing the same JSON shape — typically a timestamp, status code, an error message, and the request path. Defining one small ErrorResponse record and using it in every @ExceptionHandler keeps error handling predictable across the whole API.`,
      },
    ],
    examples: [
      {
        caption: 'A global exception handler covering a custom not-found exception and validation failures',
        code: `public class ProductNotFoundException extends RuntimeException {
    public ProductNotFoundException(Long id) {
        super("Product not found with id: " + id);
    }
}

public record ErrorResponse(LocalDateTime timestamp, int status, String message, String path) {}

@RestControllerAdvice
public class GlobalExceptionHandler {

    @ExceptionHandler(ProductNotFoundException.class)
    public ResponseEntity<ErrorResponse> handleNotFound(ProductNotFoundException ex, HttpServletRequest req) {
        ErrorResponse body = new ErrorResponse(LocalDateTime.now(), 404, ex.getMessage(), req.getRequestURI());
        return ResponseEntity.status(HttpStatus.NOT_FOUND).body(body);
    }

    @ExceptionHandler(MethodArgumentNotValidException.class)
    public ResponseEntity<ErrorResponse> handleValidation(MethodArgumentNotValidException ex, HttpServletRequest req) {
        String message = ex.getBindingResult().getFieldErrors().stream()
                .map(f -> f.getField() + ": " + f.getDefaultMessage())
                .collect(Collectors.joining(", "));
        ErrorResponse body = new ErrorResponse(LocalDateTime.now(), 400, message, req.getRequestURI());
        return ResponseEntity.badRequest().body(body);
    }

    @ExceptionHandler(Exception.class)
    public ResponseEntity<ErrorResponse> handleGeneric(Exception ex, HttpServletRequest req) {
        ErrorResponse body = new ErrorResponse(LocalDateTime.now(), 500, "Unexpected error", req.getRequestURI());
        return ResponseEntity.internalServerError().body(body);
    }
}`,
        output: `GET /api/products/999
-> 404 Not Found
{"timestamp":"2026-09-21T10:20:00","status":404,"message":"Product not found with id: 999","path":"/api/products/999"}`,
      },
    ],
    commonMistakes: [
      'Writing duplicate try-catch blocks in every controller instead of centralizing handling in one @ControllerAdvice class.',
      'Catching the generic Exception.class before more specific handlers, which prevents the specific handler from ever being reached (Spring picks the most specific match, but ordering and inheritance mistakes still cause confusion).',
      'Leaking internal details (stack traces, SQL exception messages) directly into the client-facing error response.',
      'Forgetting @RestControllerAdvice vs @ControllerAdvice, then wondering why the returned object is treated as a view name instead of a JSON body.',
    ],
    keyPoints: [
      '@ControllerAdvice (or @RestControllerAdvice) centralizes exception handling across all controllers.',
      '@ExceptionHandler(SomeException.class) methods intercept a specific exception type wherever it is thrown in request handling.',
      'Map each exception type to the correct HTTP status code rather than letting everything fall back to 500.',
      'A consistent ErrorResponse shape across all handlers makes the API predictable for clients.',
    ],
  },

  openapi: {
    title: 'API Documentation with OpenAPI (springdoc-openapi)',
    intro: `As a REST API grows, keeping documentation in sync by hand becomes unreliable. springdoc-openapi is a library that inspects your Spring MVC controllers, request/response DTOs, and validation annotations at runtime and automatically generates an OpenAPI (formerly Swagger) specification — plus a browsable Swagger UI page — without you writing separate documentation files.`,
    sections: [
      {
        heading: 'Adding springdoc-openapi',
        body: `Adding the <code>springdoc-openapi-starter-webmvc-ui</code> dependency is enough to get a working setup: once the app starts, the raw OpenAPI JSON is available at <code>/v3/api-docs</code>, and an interactive documentation UI is served at <code>/swagger-ui.html</code>, where you can browse every endpoint, see its parameters and response shapes, and even send test requests directly from the browser.`,
      },
      {
        heading: 'Enriching the Generated Documentation',
        body: `The base generation is inferred purely from your code, but annotations let you add human-readable detail: <code>@Operation(summary, description)</code> on a controller method, <code>@ApiResponse</code> to document specific status codes and their meaning, and <code>@Schema</code> on DTO fields to describe individual fields or mark examples. This turns a bare technical listing into documentation another team (or a frontend developer) can actually use without reading your source code.`,
      },
      {
        heading: 'Documenting Security Requirements',
        body: `For an API protected by JWT or another auth scheme, springdoc supports declaring a security scheme (e.g. bearer authentication) once in configuration, after which Swagger UI shows an "Authorize" button letting a developer paste in a token and have it automatically attached to every test request they send from the UI.`,
      },
    ],
    examples: [
      {
        caption: 'Enriching an endpoint with OpenAPI annotations',
        code: `@RestController
@RequestMapping("/api/products")
@Tag(name = "Products", description = "Operations for managing catalog products")
public class ProductController {

    @Operation(summary = "Get a product by ID")
    @ApiResponses({
        @ApiResponse(responseCode = "200", description = "Product found"),
        @ApiResponse(responseCode = "404", description = "Product not found")
    })
    @GetMapping("/{id}")
    public ResponseEntity<ProductResponse> getById(
            @Parameter(description = "ID of the product to retrieve") @PathVariable Long id) {
        // implementation
        return ResponseEntity.ok(new ProductResponse(id, "Wireless Mouse", 19.99));
    }
}

// pom.xml dependency:
// <dependency>
//   <groupId>org.springdoc</groupId>
//   <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
//   <version>2.5.0</version>
// </dependency>`,
        output: `Swagger UI available at: http://localhost:8080/swagger-ui.html
Raw spec available at:   http://localhost:8080/v3/api-docs
"Products" group shows GET /api/products/{id} with documented 200 and 404 responses.`,
      },
    ],
    commonMistakes: [
      'Assuming springdoc-openapi requires manually writing a YAML/JSON spec file — it generates the spec automatically from your existing controllers and DTOs.',
      'Leaving the Swagger UI publicly exposed in production without any access restriction, which reveals your full API surface to anyone.',
      'Not annotating DTOs with @Schema descriptions, leaving the generated docs technically complete but not actually explanatory for consumers.',
      'Confusing springdoc-openapi (current, active library) with the older springfox-swagger2, which is largely unmaintained for modern Spring Boot versions.',
    ],
    keyPoints: [
      'springdoc-openapi generates an OpenAPI spec and Swagger UI automatically from your controllers and DTOs.',
      '/v3/api-docs exposes the raw spec; /swagger-ui.html exposes the interactive browsable UI.',
      '@Operation, @ApiResponse, and @Schema annotations enrich the generated documentation with human-readable detail.',
      'Restrict or disable Swagger UI in production environments unless the API is intentionally public-facing.',
    ],
  },

  'spring-data-jpa': {
    title: 'Spring Data JPA',
    intro: `Spring Data JPA removes the boilerplate of writing DAO classes by hand. You define a repository interface, and Spring generates the implementation at runtime — including full CRUD operations and, for many common query shapes, even the query itself, derived directly from the method name.`,
    sections: [
      {
        heading: 'JpaRepository and Built-In CRUD',
        body: `Extending <code>JpaRepository&lt;EntityType, IdType&gt;</code> immediately gives you methods like <code>save()</code>, <code>findById()</code>, <code>findAll()</code>, <code>deleteById()</code>, and <code>count()</code> — with no implementation code required. Spring Data generates a proxy implementation at startup and registers it as a bean you can simply @Autowire or inject via constructor.`,
      },
      {
        heading: 'Derived Query Methods',
        body: `Spring Data JPA can parse a method name and build the corresponding query automatically, as long as it follows the expected keyword conventions — <code>findBy</code>, <code>And</code>, <code>Or</code>, <code>OrderBy</code>, <code>GreaterThan</code>, <code>Containing</code>, and more. For example, <code>findByEmailAndActiveTrue(String email)</code> generates a query filtering on both the email column and an active flag, with zero SQL or JPQL written by hand.`,
      },
      {
        heading: 'Custom Queries with @Query',
        body: `When a query is too complex to express as a method name, or you need specific join/aggregation logic, <code>@Query</code> lets you write JPQL (object-oriented, referencing entity fields) or native SQL (with <code>nativeQuery = true</code>) directly above the method signature, while Spring Data still handles binding the method's parameters into the query.`,
      },
    ],
    examples: [
      {
        caption: 'A repository combining CRUD, derived queries, and a custom @Query',
        code: `public interface ProductRepository extends JpaRepository<Product, Long> {

    // Derived query: SELECT * FROM product WHERE category = ? AND price < ?
    List<Product> findByCategoryAndPriceLessThan(String category, BigDecimal price);

    // Derived query with ordering
    List<Product> findByCategoryOrderByPriceAsc(String category);

    // Custom JPQL query for a case a method name can't express cleanly
    @Query("SELECT p FROM Product p WHERE p.stock < :threshold")
    List<Product> findLowStockProducts(@Param("threshold") int threshold);
}

@Service
public class ProductService {
    private final ProductRepository repository;
    public ProductService(ProductRepository repository) { this.repository = repository; }

    public List<Product> cheapElectronics() {
        return repository.findByCategoryAndPriceLessThan("Electronics", new BigDecimal("50.00"));
    }
}`,
        output: `cheapElectronics() ->
[Product{id=3, name='USB Cable', price=8.99}, Product{id=7, name='Phone Stand', price=14.50}]`,
      },
    ],
    commonMistakes: [
      'Misspelling an entity field name inside a derived query method name — Spring Data fails at application startup with a clear but often overlooked "No property X found" error.',
      'Writing overly long derived method names for complex filters instead of switching to @Query once readability suffers.',
      'Forgetting @Param in a @Query method when using named parameters (:name) instead of positional ones.',
      'Calling findById() and directly using .get() on the returned Optional without checking presence, risking a NoSuchElementException.',
    ],
    keyPoints: [
      'JpaRepository provides full CRUD operations with zero implementation code.',
      'Derived query methods build queries automatically from a method name following Spring Data\'s keyword conventions.',
      '@Query supports JPQL or native SQL for queries too complex to express as a method name.',
      'Repository interfaces are auto-implemented as proxy beans at application startup.',
    ],
  },

  hibernate: {
    title: 'Hibernate and JPA Entity Mapping',
    intro: `Hibernate is the JPA (Jakarta Persistence API) implementation that Spring Boot uses by default under Spring Data JPA. JPA defines the standard annotations and interfaces; Hibernate is the engine that actually translates your annotated Java classes into SQL and manages the object-relational mapping at runtime.`,
    sections: [
      {
        heading: 'Core Entity Annotations',
        body: `A class becomes a persistent entity through a small set of annotations that describe how it maps to a database table and columns.`,
        list: [
          '<code>@Entity</code> — marks a class as a JPA-managed entity, mapped to a database table.',
          '<code>@Id</code> — marks the primary key field; <code>@GeneratedValue</code> configures how it is generated (IDENTITY, SEQUENCE, AUTO).',
          '<code>@Column</code> — customizes a field\'s mapped column name, length, nullability, or uniqueness (optional if defaults are acceptable).',
          '<code>@Table</code> — customizes the mapped table name when it should differ from the class name.',
        ],
      },
      {
        heading: 'Mapping Relationships',
        body: `JPA models real-world relationships between entities using dedicated annotations: <code>@OneToMany</code>/<code>@ManyToOne</code> for parent-child relationships (e.g. one Customer has many Orders), <code>@ManyToMany</code> for relationships needing a join table (e.g. Students and Courses), and <code>@OneToOne</code> for a strict one-to-one link. The "owning side" of a relationship (the one holding the foreign key) is marked with <code>mappedBy</code> on the inverse side to avoid Hibernate trying to manage the relationship from both directions.`,
      },
      {
        heading: 'Fetch Types and Lazy Loading',
        body: `Every association has a fetch strategy — <code>FetchType.LAZY</code> (load on first access) or <code>FetchType.EAGER</code> (load immediately with the parent). @ManyToOne and @OneToOne default to EAGER; @OneToMany and @ManyToMany default to LAZY. Lazy associations accessed outside an active persistence context throw a LazyInitializationException, which is one of the most common runtime errors in JPA-based applications.`,
      },
    ],
    examples: [
      {
        caption: 'A one-to-many relationship between Customer and Order',
        code: `@Entity
public class Customer {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(nullable = false)
    private String name;

    @OneToMany(mappedBy = "customer", cascade = CascadeType.ALL)
    private List<Order> orders = new ArrayList<>();
}

@Entity
public class Order {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    private BigDecimal total;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "customer_id")
    private Customer customer;
}`,
        output: `Hibernate: create table customer (id bigint generated by default as identity, name varchar(255) not null, primary key (id))
Hibernate: create table orders (id bigint generated by default as identity, total numeric(19,2), customer_id bigint, primary key (id))
Hibernate: alter table orders add constraint FK... foreign key (customer_id) references customer`,
      },
    ],
    commonMistakes: [
      'Accessing a LAZY collection or association after the transaction/session has already closed, causing a LazyInitializationException.',
      'Forgetting mappedBy on the inverse side of a bidirectional relationship, which causes Hibernate to create an unwanted extra join table.',
      'Using FetchType.EAGER everywhere "to be safe," which quietly loads far more data than needed and hurts performance at scale.',
      'Not setting cascade carefully on @OneToMany, leading to either orphaned child rows or unintended deletions when the parent changes.',
    ],
    keyPoints: [
      'Hibernate is the default JPA implementation used by Spring Data JPA in Spring Boot.',
      '@Entity, @Id, @GeneratedValue, and @Column define how a class maps to a table.',
      '@OneToMany, @ManyToOne, @ManyToMany, and @OneToOne map relationships between entities, with mappedBy identifying the inverse side.',
      'LAZY vs EAGER fetch strategy controls when related data loads, and mismanaging it is a very common source of bugs.',
    ],
  },

  pagination: {
    title: 'Pagination and Sorting with Pageable',
    intro: `Returning an entire table in one response does not scale — a products table with 500,000 rows should never be fetched in a single query for a listing page. Spring Data JPA builds pagination and sorting directly into the repository abstraction through the Pageable and Page types, so paging works consistently across every repository without custom SQL.`,
    sections: [
      {
        heading: 'Pageable and PageRequest',
        body: `<code>Pageable</code> is an interface describing a page request: which page number, how many items per page, and optional sort order. <code>PageRequest.of(page, size, sort)</code> is the standard way to construct one. Any repository method can accept a Pageable parameter as long as it either overrides a JpaRepository method or is declared to return a Page/Slice/List.`,
      },
      {
        heading: 'The Page<T> Response',
        body: `A repository method returning <code>Page&lt;T&gt;</code> gives back not just the requested slice of data but also metadata: total number of elements, total number of pages, whether this is the first/last page, and the current page number. This is exactly the metadata a frontend needs to render pagination controls without a separate count query.`,
      },
      {
        heading: 'Accepting Pagination Parameters from the Client',
        body: `Spring MVC can bind an incoming Pageable directly from query parameters (e.g. <code>?page=0&size=20&sort=price,desc</code>) when a controller method parameter is typed as Pageable — Spring Data Web support resolves it automatically, so you rarely construct PageRequest by hand inside a controller.`,
      },
    ],
    examples: [
      {
        caption: 'Paginating and sorting a product listing endpoint',
        code: `public interface ProductRepository extends JpaRepository<Product, Long> {
    Page<Product> findByCategory(String category, Pageable pageable);
}

@RestController
@RequestMapping("/api/products")
public class ProductController {
    private final ProductRepository repository;
    public ProductController(ProductRepository repository) { this.repository = repository; }

    @GetMapping
    public Page<Product> list(@RequestParam String category, Pageable pageable) {
        return repository.findByCategory(category, pageable);
    }
}

// GET /api/products?category=Electronics&page=0&size=2&sort=price,desc`,
        output: `{
  "content": [
    {"id":9,"name":"4K Monitor","price":299.99},
    {"id":4,"name":"Mechanical Keyboard","price":89.00}
  ],
  "pageable": {"pageNumber":0,"pageSize":2},
  "totalElements": 14,
  "totalPages": 7,
  "first": true,
  "last": false
}`,
      },
    ],
    commonMistakes: [
      'Fetching the entire table into memory and paginating it in Java code instead of pushing LIMIT/OFFSET down to the database via Pageable.',
      'Confusing Page (includes total count, an extra query) with Slice (no total count, cheaper) when only "has more" is actually needed.',
      'Not capping the client-supplied page size, allowing a request like ?size=1000000 to load an unreasonable amount of data.',
      'Assuming page numbers are 1-based — Spring Data\'s Pageable page numbers are 0-based.',
    ],
    keyPoints: [
      'Pageable describes a page request (number, size, sort); PageRequest.of() is the standard way to build one.',
      'Page<T> returns the requested data slice plus pagination metadata (total elements, total pages, first/last).',
      'A Pageable controller parameter is automatically bound from query parameters like page, size, and sort.',
      'Page numbers in Spring Data are zero-based, not one-based.',
    ],
  },

  'security-concepts': {
    title: 'Spring Security Basics',
    intro: `Spring Security is the standard framework for securing Spring Boot applications. It's built around a chain of servlet filters that intercept every incoming request before it reaches your controllers, deciding whether the request is allowed to proceed. Understanding the filter chain, and the distinction between authentication and authorization, is the foundation for everything else in Spring Security.`,
    sections: [
      {
        heading: 'The Security Filter Chain',
        body: `When spring-boot-starter-security is on the classpath, Spring Boot registers a chain of servlet filters in front of your application (via <code>DelegatingFilterProxy</code> and <code>FilterChainProxy</code>). Each filter has one responsibility — one checks for a session, one checks for HTTP Basic credentials, one handles CSRF, one handles authorization decisions — and a request must pass through the whole chain before reaching a controller. By default, adding the security starter alone makes every endpoint require authentication with a generated login form and a random password printed to the console at startup.`,
      },
      {
        heading: 'Authentication vs Authorization',
        body: `These two terms are often used loosely but mean distinctly different things. <strong>Authentication</strong> answers "who are you?" — verifying credentials (username/password, a token) and establishing an identity. <strong>Authorization</strong> answers "what are you allowed to do?" — given a known identity, deciding whether that identity may access a specific resource or perform a specific action, typically based on roles or authorities.`,
      },
      {
        heading: 'Configuring Access Rules',
        body: `Modern Spring Security (5.7+) configures access rules through a <code>SecurityFilterChain</code> @Bean rather than extending WebSecurityConfigurerAdapter (deprecated and removed in newer versions). Inside it, <code>authorizeHttpRequests()</code> declares which paths require authentication, which require a specific role, and which are open to everyone.`,
      },
    ],
    examples: [
      {
        caption: 'A modern SecurityFilterChain configuration with role-based authorization',
        code: `@Configuration
@EnableWebSecurity
public class SecurityConfig {

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/api/public/**").permitAll()
                .requestMatchers("/api/admin/**").hasRole("ADMIN")
                .requestMatchers(HttpMethod.GET, "/api/products/**").permitAll()
                .anyRequest().authenticated()
            )
            .csrf(csrf -> csrf.disable()) // typically disabled for stateless REST APIs
            .sessionManagement(sm -> sm.sessionCreationPolicy(SessionCreationPolicy.STATELESS));
        return http.build();
    }

    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}`,
        output: `GET  /api/public/info     -> 200 OK (no auth required)
GET  /api/products/5      -> 200 OK (no auth required)
GET  /api/admin/reports   -> 403 Forbidden (authenticated but missing ROLE_ADMIN)
GET  /api/orders          -> 401 Unauthorized (no credentials supplied)`,
      },
    ],
    commonMistakes: [
      'Confusing 401 Unauthorized (authentication is missing or invalid) with 403 Forbidden (authenticated, but not authorized for this resource).',
      'Storing plaintext passwords instead of hashing them with a PasswordEncoder like BCryptPasswordEncoder.',
      'Leaving CSRF protection enabled on a stateless, token-based REST API where it provides no benefit and only blocks legitimate non-browser clients.',
      'Extending the deprecated WebSecurityConfigurerAdapter in a new project instead of the current SecurityFilterChain bean approach.',
    ],
    keyPoints: [
      'Spring Security enforces access control through a chain of servlet filters that run before your controllers.',
      'Authentication establishes identity ("who are you"); authorization decides access ("what are you allowed to do").',
      'SecurityFilterChain with authorizeHttpRequests() is the current, non-deprecated way to configure access rules.',
      'Passwords must always be hashed (e.g. with BCryptPasswordEncoder), never stored or compared as plaintext.',
    ],
  },

  'jwt-concepts': {
    title: 'JWT Authentication Concepts in Spring Boot',
    intro: `Traditional session-based authentication stores login state on the server, which doesn't scale cleanly across multiple server instances without shared session storage. JSON Web Tokens (JWT) solve this by putting the identity and claims directly inside a signed token that the client stores and sends with every request — the server verifies the signature and needs to keep no session state at all.`,
    sections: [
      {
        heading: 'Structure of a JWT',
        body: `A JWT is a string made of three Base64URL-encoded parts separated by dots: <code>header.payload.signature</code>. The header specifies the signing algorithm (e.g. HS256). The payload holds claims — data such as the subject (username), issued-at time, expiration time, and any custom claims like roles. The signature is computed over the header and payload using a secret key (or private key for asymmetric algorithms), and it's what lets the server detect if a token has been tampered with — without the signature verifying, the token is rejected outright.`,
      },
      {
        heading: 'The Stateless Authentication Flow',
        body: `The typical flow has distinct steps: the client sends credentials to a login endpoint; the server verifies them and issues a signed JWT; the client stores the token (commonly in memory or a secure cookie) and attaches it as an <code>Authorization: Bearer &lt;token&gt;</code> header on every subsequent request; a security filter on the server extracts and validates the token on each request, and if valid, populates the SecurityContext with the authenticated user — all without querying a session store.`,
      },
      {
        heading: 'JWTs Are Not Encrypted by Default',
        body: `A standard JWT (JWS — signed, not JWE — encrypted) is only signed, not encrypted — anyone can Base64-decode the payload and read its contents, they simply cannot modify it undetected without invalidating the signature. This means JWTs must never carry sensitive data like passwords, and expiration times must be kept short since a stolen valid token remains usable until it expires (unless a revocation/blacklist mechanism is added).`,
      },
    ],
    examples: [
      {
        caption: 'A JWT authentication filter that validates a bearer token on each request',
        code: `public class JwtAuthFilter extends OncePerRequestFilter {

    private final JwtService jwtService;
    public JwtAuthFilter(JwtService jwtService) { this.jwtService = jwtService; }

    @Override
    protected void doFilterInternal(HttpServletRequest request, HttpServletResponse response,
                                     FilterChain chain) throws ServletException, IOException {
        String header = request.getHeader("Authorization");
        if (header != null && header.startsWith("Bearer ")) {
            String token = header.substring(7);
            if (jwtService.isValid(token)) {
                String username = jwtService.extractUsername(token);
                var authToken = new UsernamePasswordAuthenticationToken(username, null,
                        jwtService.extractAuthorities(token));
                SecurityContextHolder.getContext().setAuthentication(authToken);
            }
        }
        chain.doFilter(request, response);
    }
}

// Example decoded JWT payload:
// { "sub": "asha@webnest.com", "roles": ["USER"], "iat": 1758441600, "exp": 1758445200 }`,
        output: `Request with valid, unexpired token -> reaches controller as authenticated user "asha@webnest.com"
Request with expired token -> 401 Unauthorized, SecurityContext left unauthenticated
Request with tampered payload -> signature check fails, token rejected before claims are trusted`,
      },
    ],
    commonMistakes: [
      'Storing sensitive data (passwords, credit card numbers) inside JWT claims, forgetting that the payload is only Base64-encoded, not encrypted.',
      'Setting an extremely long or infinite expiration time, which makes a stolen token dangerous for a long window with no way to revoke it centrally.',
      'Skipping signature verification and trusting the decoded payload directly, which allows a forged token with fabricated claims to pass as valid.',
      'Storing the JWT in localStorage on the frontend without considering XSS risk, when an HttpOnly cookie may be a safer storage choice for browser clients.',
    ],
    keyPoints: [
      'A JWT has three parts — header, payload, signature — and the signature lets the server detect tampering.',
      'Stateless JWT auth: login issues a token, the client sends it as a Bearer header, and a filter validates it on every request without server-side session storage.',
      'A standard JWT is signed but not encrypted — its payload is readable by anyone, so it must never contain sensitive data.',
      'Short expiration times limit the damage window if a token is stolen, since revoking an already-issued JWT is not built in by default.',
    ],
  },

  actuator: {
    title: 'Spring Boot Actuator',
    intro: `Once an application is running in production, you need visibility into its health, performance, and internal state without attaching a debugger. Spring Boot Actuator adds a set of production-ready HTTP endpoints — health checks, metrics, environment details, and more — with almost no configuration required beyond adding the starter.`,
    sections: [
      {
        heading: 'Adding Actuator and the /actuator/health Endpoint',
        body: `Adding <code>spring-boot-starter-actuator</code> exposes a base <code>/actuator</code> endpoint listing available sub-endpoints. The most commonly used one, <code>/actuator/health</code>, reports whether the application (and its key dependencies — database, disk space, message brokers) is UP or DOWN, which is exactly what load balancers and orchestration platforms like Kubernetes poll to decide whether an instance should keep receiving traffic.`,
      },
      {
        heading: 'Metrics with /actuator/metrics',
        body: `<code>/actuator/metrics</code> exposes a catalog of runtime metrics — JVM memory usage, garbage collection pauses, HTTP request counts and latencies, thread pool statistics, and datasource connection pool usage. Requesting a specific metric name (e.g. <code>/actuator/metrics/http.server.requests</code>) returns detailed measurements for that metric. Actuator integrates with Micrometer, which can export the same metrics to Prometheus, Datadog, or other monitoring backends.`,
      },
      {
        heading: 'Controlling Exposure',
        body: `By default, only <code>/health</code> and <code>/info</code> are exposed over HTTP for security reasons — most other endpoints (env, beans, mappings, threaddump) reveal internal detail that should not be public. Exposure is controlled with the <code>management.endpoints.web.exposure.include</code> property, and sensitive endpoints should additionally be secured behind authentication and, ideally, a separate management port in production.`,
      },
    ],
    examples: [
      {
        caption: 'Enabling and querying Actuator health and metrics endpoints',
        code: `# application.properties
management.endpoints.web.exposure.include=health,metrics,info
management.endpoint.health.show-details=when-authorized
management.info.env.enabled=true

info.app.name=Webnest Product API
info.app.version=1.4.0`,
        output: `GET /actuator/health
-> {"status":"UP","components":{"db":{"status":"UP"},"diskSpace":{"status":"UP"}}}

GET /actuator/info
-> {"app":{"name":"Webnest Product API","version":"1.4.0"}}

GET /actuator/metrics/jvm.memory.used
-> {"name":"jvm.memory.used","measurements":[{"statistic":"VALUE","value":1.87E8}]}`,
      },
    ],
    commonMistakes: [
      'Exposing all Actuator endpoints (management.endpoints.web.exposure.include=*) publicly in production, revealing environment variables, bean definitions, and internal request mappings to anyone.',
      'Relying on /actuator/health without show-details configured, then being surprised the JSON gives only "UP"/"DOWN" with no component breakdown.',
      'Never securing Actuator endpoints with Spring Security, leaving operational internals unauthenticated.',
      'Forgetting that /actuator/health checks configured components (database, disk) and can report DOWN even when the web server itself is running fine.',
    ],
    keyPoints: [
      'spring-boot-starter-actuator adds production-ready monitoring endpoints with minimal setup.',
      '/actuator/health reports UP/DOWN status for the app and its key dependencies, commonly polled by load balancers.',
      '/actuator/metrics exposes JVM, HTTP, and datasource metrics, and integrates with Micrometer for external monitoring tools.',
      'Only health and info are exposed by default; explicitly opt in to other endpoints and secure them appropriately.',
    ],
  },

  logging: {
    title: 'Logging in Spring Boot',
    intro: `Print statements do not scale for real applications — you need log levels, structured output, and the ability to turn verbosity up or down per package without redeploying code. Spring Boot ships with a logging abstraction (SLF4J as the facade, Logback as the default implementation) already configured, so logging works out of the box with sensible console output.`,
    sections: [
      {
        heading: 'SLF4J as the Logging Facade',
        body: `SLF4J (Simple Logging Facade for Java) provides a common logging API that decouples your code from a specific logging implementation. You write against the SLF4J <code>Logger</code> interface, and Spring Boot wires up Logback underneath by default — meaning you could swap in Log4j2 later without changing a single log statement in your code.`,
      },
      {
        heading: 'Log Levels',
        body: `Logging is organized by severity level, from most to least verbose: TRACE, DEBUG, INFO, WARN, ERROR. Setting a level for a logger (or package) means that level and everything more severe will be emitted; more verbose levels are suppressed. Spring Boot defaults the root logger to INFO, which is why routine framework startup messages appear but detailed debug traces do not, unless you explicitly raise verbosity.`,
      },
      {
        heading: 'Configuring Log Levels per Package',
        body: `Log levels are configured through properties using the <code>logging.level.*</code> prefix, letting you set different verbosity for different parts of the application — for example, keeping your own code at DEBUG while leaving noisy third-party libraries at WARN. This is far more useful than a single global level, especially when diagnosing an issue in one specific layer (e.g. Hibernate SQL logging) without flooding logs from everything else.`,
      },
    ],
    examples: [
      {
        caption: 'Using SLF4J logging with per-package level configuration',
        code: `# application.properties
logging.level.root=INFO
logging.level.com.webnest.app=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.springframework.web=WARN

---

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@Service
public class OrderService {
    private static final Logger log = LoggerFactory.getLogger(OrderService.class);

    public void placeOrder(Order order) {
        log.debug("Processing order for customer {}", order.getCustomerId());
        try {
            // process
            log.info("Order {} placed successfully", order.getId());
        } catch (Exception ex) {
            log.error("Failed to place order for customer {}", order.getCustomerId(), ex);
        }
    }
}`,
        output: `2026-09-21 10:32:11.502 DEBUG 8412 --- [nio-8080-exec-1] c.w.app.OrderService : Processing order for customer 42
2026-09-21 10:32:11.548 INFO  8412 --- [nio-8080-exec-1] c.w.app.OrderService : Order 1007 placed successfully`,
      },
    ],
    commonMistakes: [
      'Using System.out.println() instead of a proper logger, which loses log levels, timestamps, and the ability to redirect/filter output.',
      'Building log messages by string concatenation instead of SLF4J\'s parameterized {} placeholders, which wastes CPU formatting strings even when the log level would filter them out.',
      'Logging entire exception objects with ex.toString() instead of passing the exception as the last argument, which loses the full stack trace in the output.',
      'Leaving DEBUG or TRACE enabled globally in production, generating excessive log volume and potentially leaking sensitive request/response data.',
    ],
    keyPoints: [
      'Spring Boot uses SLF4J as a logging facade with Logback as the default implementation, working out of the box.',
      'Log levels (TRACE, DEBUG, INFO, WARN, ERROR) control verbosity, with INFO as the default root level.',
      'logging.level.<package>=LEVEL configures verbosity independently per package.',
      'Use parameterized logging ("{}", value) instead of string concatenation, and pass exceptions as the final argument to preserve the stack trace.',
    ],
  },

  testing: {
    title: 'Testing Spring Boot Applications',
    intro: `Spring Boot applications benefit from testing at different levels of the stack — a full integration test that boots the entire application context is thorough but slow, while a narrowly focused slice test loads only what's needed for one layer and runs much faster. Spring Boot Test provides annotations for exactly this range of granularity, built on JUnit 5, Mockito, and AssertJ (all bundled in spring-boot-starter-test).`,
    sections: [
      {
        heading: '@SpringBootTest — Full Integration Tests',
        body: `<code>@SpringBootTest</code> boots the entire Spring ApplicationContext, exactly like the real running application, making it suitable for true end-to-end integration tests. It's the slowest test type since it wires up every bean, so it should be used deliberately rather than for every test class — typically for a handful of tests that verify the whole system works together, often combined with <code>webEnvironment = RANDOM_PORT</code> and a real HTTP client for true black-box testing.`,
      },
      {
        heading: '@WebMvcTest — Controller Slice Tests',
        body: `<code>@WebMvcTest(ControllerClass.class)</code> loads only the Spring MVC infrastructure and the specified controller, without starting the full context (no real database, no service beans unless mocked). Combined with <code>MockMvc</code>, it lets you send simulated HTTP requests and assert on status codes, headers, and JSON response bodies quickly, while service dependencies are typically supplied as <code>@MockBean</code>.`,
      },
      {
        heading: '@DataJpaTest — Repository Slice Tests',
        body: `<code>@DataJpaTest</code> configures only the JPA-related components — an in-memory embedded database (by default), your repository interfaces, and Hibernate — without loading controllers or unrelated services. Each test method runs inside a transaction that's rolled back afterward, keeping tests isolated from each other without manual cleanup.`,
      },
    ],
    examples: [
      {
        caption: 'A controller slice test with MockMvc and a repository slice test with @DataJpaTest',
        code: `@WebMvcTest(ProductController.class)
class ProductControllerTest {

    @Autowired
    private MockMvc mockMvc;

    @MockBean
    private ProductService productService;

    @Test
    void getById_returnsProduct_whenFound() throws Exception {
        when(productService.findById(5L))
                .thenReturn(Optional.of(new Product(5L, "Wireless Mouse", new BigDecimal("19.99"))));

        mockMvc.perform(get("/api/products/5"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.name").value("Wireless Mouse"));
    }
}

@DataJpaTest
class ProductRepositoryTest {

    @Autowired
    private ProductRepository repository;

    @Test
    void findByCategory_returnsMatchingProducts() {
        repository.save(new Product(null, "Keyboard", "Electronics", new BigDecimal("45.00")));
        repository.save(new Product(null, "Desk Lamp", "Home", new BigDecimal("22.00")));

        List<Product> results = repository.findByCategory("Electronics");

        assertThat(results).hasSize(1);
        assertThat(results.get(0).getName()).isEqualTo("Keyboard");
    }
}`,
        output: `ProductControllerTest > getById_returnsProduct_whenFound() PASSED
ProductRepositoryTest > findByCategory_returnsMatchingProducts() PASSED

Tests run: 2, Failures: 0, Errors: 0, Skipped: 0`,
      },
    ],
    commonMistakes: [
      'Using @SpringBootTest for every test class, causing the full application context to boot repeatedly and slowing the test suite dramatically as the project grows.',
      'Forgetting @MockBean for a controller\'s dependencies inside @WebMvcTest, causing context startup failures because the real service bean (and its own dependencies, like a database) is not available in that slice.',
      'Testing the repository layer against production-like assumptions without realizing @DataJpaTest uses an embedded in-memory database by default, whose SQL dialect quirks can differ subtly from the real production database.',
      'Not resetting shared mutable state between tests, causing flaky, order-dependent test failures.',
    ],
    keyPoints: [
      '@SpringBootTest boots the full application context — thorough but slow, best used for true integration tests.',
      '@WebMvcTest loads only MVC infrastructure and one controller, paired with MockMvc and @MockBean for fast controller tests.',
      '@DataJpaTest loads only JPA components with an embedded database and per-test transaction rollback, ideal for repository tests.',
      'spring-boot-starter-test bundles JUnit 5, Mockito, and AssertJ so no extra setup is needed to start testing.',
    ],
  },

  'docker-deployment-concepts': {
    title: 'Docker and Deployment Concepts for Spring Boot',
    intro: `Packaging a Spring Boot application as a Docker image makes it runnable identically on any machine with Docker installed — a developer's laptop, a CI pipeline, or a production Kubernetes cluster — without worrying about differing Java versions or OS-level dependencies. Spring Boot's default packaging (a self-contained "fat jar" via the Maven/Gradle plugin) makes this especially straightforward.`,
    sections: [
      {
        heading: 'Packaging as an Executable Jar',
        body: `Running <code>mvn package</code> (or <code>./gradlew build</code>) with the Spring Boot plugin produces a single executable jar containing your compiled classes plus every dependency, runnable directly with <code>java -jar app.jar</code> — no separate application server or classpath setup required. This is exactly what gets copied into a Docker image.`,
      },
      {
        heading: 'A Multi-Stage Dockerfile',
        body: `A multi-stage build uses one stage with a full JDK and build tool to compile and package the application, then copies only the resulting jar into a second, much smaller stage based on a minimal JRE (not JDK) base image. This keeps the final image small (no build tools, no source code, no Maven cache) and reduces the attack surface, since the shipped image only needs a runtime, not a development toolchain.`,
      },
      {
        heading: 'Deployment Considerations',
        body: `In production, containers are typically run under an orchestrator (Kubernetes, ECS, etc.) that expects the container to expose a health check (Actuator's /actuator/health fits this role well), receive configuration through environment variables rather than baked-in files, and shut down gracefully on a SIGTERM signal so in-flight requests can finish (Spring Boot supports graceful shutdown via <code>server.shutdown=graceful</code>).`,
      },
    ],
    examples: [
      {
        caption: 'A multi-stage Dockerfile for a Spring Boot application',
        code: `# --- Stage 1: build ---
FROM eclipse-temurin:21-jdk AS build
WORKDIR /app
COPY pom.xml .
COPY src ./src
RUN --mount=type=cache,target=/root/.m2 mvn -q -DskipTests package

# --- Stage 2: run ---
FROM eclipse-temurin:21-jre
WORKDIR /app
COPY --from=build /app/target/webnest-app-0.0.1-SNAPSHOT.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]

# Build and run:
# docker build -t webnest-app .
# docker run -p 8080:8080 -e SPRING_PROFILES_ACTIVE=prod webnest-app`,
        output: `[+] Building 42.3s (12/12) FINISHED
 => exporting to image
 => => naming to docker.io/library/webnest-app

$ docker run -p 8080:8080 -e SPRING_PROFILES_ACTIVE=prod webnest-app
... The following 1 profile is active: "prod"
... Tomcat started on port(s): 8080 (http)
... Started WebnestAppApplication in 2.13 seconds`,
      },
    ],
    commonMistakes: [
      'Using a single-stage Dockerfile based on a full JDK image for the final runtime, producing a needlessly large image that also ships the build toolchain.',
      'Baking environment-specific configuration (database URLs, secrets) directly into the image instead of injecting them at runtime via environment variables or a secrets manager.',
      'Not handling SIGTERM gracefully, causing in-flight requests to be dropped abruptly whenever the container is stopped or rescheduled.',
      'Forgetting to set an explicit JVM memory limit relative to the container\'s memory limit, risking the container being OOM-killed by the host.',
    ],
    keyPoints: [
      'Spring Boot packages a self-contained executable jar, which is exactly what a Docker image needs to run the app.',
      'A multi-stage Dockerfile builds with a full JDK image, then ships only the jar on a minimal JRE base image for a smaller, safer runtime image.',
      'Configuration should be injected at container run time (environment variables, profiles) rather than baked into the image.',
      'Production containers should expose a health check and shut down gracefully on SIGTERM.',
    ],
  },

  'production-configuration': {
    title: 'Production Configuration Best Practices for Spring Boot',
    intro: `Moving a Spring Boot application from a laptop to production means changing how configuration is managed: secrets can no longer live in a properties file committed to source control, logging needs to be less noisy, and the application needs to be observable and resilient to restarts. Spring Boot's externalized configuration model is what makes all of this possible without code changes.`,
    sections: [
      {
        heading: 'Externalizing Configuration',
        body: `Spring Boot resolves configuration from multiple sources in a defined precedence order, roughly: command-line arguments, environment variables, profile-specific files, then the base application.properties/yml, with higher-precedence sources overriding lower ones. In production, the recommended practice is to keep application.properties committed with safe defaults, and inject anything environment-specific or secret (database credentials, API keys) through environment variables or a secrets manager at deploy time.`,
      },
      {
        heading: 'Using Environment Variables',
        body: `Spring Boot's relaxed binding automatically maps environment variables to the equivalent property — <code>SPRING_DATASOURCE_URL</code> maps to <code>spring.datasource.url</code>, and <code>SERVER_PORT</code> maps to <code>server.port</code>. This lets container orchestration platforms and CI/CD pipelines inject configuration without the application needing any awareness of where it's actually deployed.`,
      },
      {
        heading: 'Production-Oriented Settings',
        body: `A handful of practical adjustments distinguish a production configuration from a development one: setting <code>spring.profiles.active=prod</code>, disabling Swagger UI or restricting it, raising log levels to WARN/INFO for verbose libraries, enabling connection pool tuning (HikariCP settings like <code>maximum-pool-size</code>), restricting exposed Actuator endpoints, and enabling <code>server.shutdown=graceful</code> so deployments and restarts do not drop in-flight requests.`,
        list: [
          'Never commit real secrets to application.properties in version control — use environment variables, a vault, or a cloud secrets manager instead.',
          'Set explicit connection pool sizes rather than relying on defaults tuned for development-scale traffic.',
          'Expose only the Actuator endpoints actually needed operationally, and secure them.',
          'Configure appropriate log levels and, ideally, structured (JSON) log output for aggregation tools.',
        ],
      },
    ],
    examples: [
      {
        caption: 'A production properties file relying on environment variables for secrets',
        code: `# application-prod.properties
spring.datasource.url=\${SPRING_DATASOURCE_URL}
spring.datasource.username=\${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=\${SPRING_DATASOURCE_PASSWORD}
spring.datasource.hikari.maximum-pool-size=20

server.shutdown=graceful
spring.lifecycle.timeout-per-shutdown-phase=30s

management.endpoints.web.exposure.include=health,metrics
management.endpoint.health.show-details=never

logging.level.root=WARN
logging.level.com.webnest.app=INFO

# Launched with:
# SPRING_PROFILES_ACTIVE=prod \\
# SPRING_DATASOURCE_URL=jdbc:postgresql://db.internal:5432/webnest \\
# SPRING_DATASOURCE_USERNAME=app_user \\
# SPRING_DATASOURCE_PASSWORD=**** \\
# java -jar app.jar`,
        output: `... The following 1 profile is active: "prod"
... HikariPool-1 - Start completed. maximumPoolSize=20
... Tomcat started on port(s): 8080 (http)
... Started WebnestAppApplication in 3.05 seconds
(On SIGTERM) ... Commencing graceful shutdown. Waiting for active requests to complete`,
      },
    ],
    commonMistakes: [
      'Committing real database passwords or API keys directly into application-prod.properties in version control.',
      'Leaving development-tuned defaults (small connection pools, verbose DEBUG logging, exposed management endpoints) unchanged when deploying to production.',
      'Not testing the "prod" profile locally before deployment, discovering missing or misspelled environment variable names only after a failed rollout.',
      'Ignoring graceful shutdown configuration, causing dropped requests and inconsistent data during routine deployments or autoscaling events.',
    ],
    keyPoints: [
      'Spring Boot resolves configuration from multiple sources with a defined precedence — environment variables and command-line arguments override file-based properties.',
      'Secrets belong in environment variables or a secrets manager, never committed to application.properties in source control.',
      'Production configuration should tune connection pools, restrict Actuator exposure, and reduce log verbosity compared to development defaults.',
      'server.shutdown=graceful lets in-flight requests finish cleanly during restarts and deployments.',
    ],
  },
}
